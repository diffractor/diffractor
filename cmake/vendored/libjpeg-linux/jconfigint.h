/* jconfigint.h for Diffractor's libjpeg-turbo build.
 *
 * All configuration lives in jconfig.h, which every internal source already reaches through
 * jpeglib.h / jinclude.h. This exists only to satisfy the sources that include it directly, and
 * mirrors what the MSVC build does.
 */
#include "jconfig.h"
